class_name Enums

enum GamePhase {
	MAIN_MENU,
	INTRO,
	RACING,
	SUCCESS,
	FAILURE
}

enum PowderState {
	IDLE,
	SPARK,
	SPARK_LOCKED,
	CONSUMED,
}

enum PowderType {

}

enum ModifierOperation {
	MULTIPLY,
	ADD
}
